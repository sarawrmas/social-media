query Posts ($limit: Int!, $cursor: String) {
  posts (limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      postTitle
      textSnippet
      points
      createdAt
      updatedAt
      creator {
        id
        username
      }
    }
  }
}